<div class="container">
  <h2>My Groups</h2>
  <div>
    <% @groups.each do |group| %>
        <li><%= link_to group.name, group_path(group) %></li>
    <% end %>
  </div>
  <%= link_to new_group_path do %>
    <button type="button" class="btn btn-success">Create new group</button>
  <% end %>
</div>
